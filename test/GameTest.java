import org.junit.Test;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.HashSet;
import java.util.List;

import static org.junit.Assert.*;

public class GameTest {

	@Test
	public void clearScreen() {
	}

	@Test
	public void initialise() {
	/*	List<String> characters = new ArrayList<>(Arrays.asList(config.getProperty("Characters", Game.Settings.DEFAULT_CHARACTERS).split(",")));
		List<String> weapons = new ArrayList<>(Arrays.asList(config.getProperty("Weapons", Game.Settings.DEFAULT_WEAPONS).split(",")));
		Game g = new Game();
		g.playerList.add(new Human(characters.remove(j - 1), new HashSet<>(),
				b.startTiles.remove(0), this, new DetectiveCard(mCards)));
	*/}

	@Test
	public void doSuggestion() {
	}

	@Test
	public void doAccusation() {
	}

	@Test
	public void showBoard() {
	}
}